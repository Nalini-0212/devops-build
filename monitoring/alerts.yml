groups:
  - name: application-alerts

    rules:
      - alert: ApplicationDown
        expr: probe_success == 0
        for: 1m

        labels:
          severity: critical

        annotations:
          summary: "React Application is Down"
          description: "Application is unreachable for more than 1 minute."